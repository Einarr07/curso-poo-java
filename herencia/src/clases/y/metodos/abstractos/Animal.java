package clases.y.metodos.abstractos;

public abstract class Animal extends SerVivo {
    /*
     * Esta clase "Animal" extiende de "SerVivo" y hereda el método abstracto "alimentarse()",
     * pero no lo implementa aquí, porque es una clase abstracta. Las subclases que extienden de "Animal"
     * deberán proporcionar la implementación específica del método "alimentarse()".
     */
}
